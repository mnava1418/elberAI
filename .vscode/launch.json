{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Auth Services",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}/backEnd/auth-services"
        },       
    ]
}